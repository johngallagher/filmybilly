<div>
  <h1>Listing reviews</h1>
  <ul id="reviews">
    <% @reviews.each do |review| %>
      <% @review = review %>
      <%= render 'show' %>
    <% end %>
  </ul>
  <br />
  <%= link_to 'New Review', new_review_path %>
</div>
